"use strict";

/*  Basic ex 1

Make a multiplication table using TypeScript loops from an array with
 number values 1 to 10 and the result should look like this:

 */
for (var i = 0; i < 10; i++) {
  document.write([i] + "x1" + [i]);
}

var message = 'Hello World';
console.log(message);